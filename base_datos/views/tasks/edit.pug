doctype html
html
    head
        title Tareas
    body
        form(action=`/tasks/${task.id}?_method=PUT` method="POST")
            input(type="text" name="categories" placeholder="ID de categorias separado por coma")
            textarea(name="description" placeholder="Escribe la descripción")= task.description
            input(type="submit" value="Guardar")